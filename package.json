{
  "name": "starsmaid",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "check": "node --check app.js && node --check sw.js && node --check server.js && node --check functions/index.js",
    "firebase:serve": "firebase emulators:start --only hosting,functions,firestore",
    "firebase:deploy": "firebase deploy --only hosting,functions,firestore:rules"
  },
  "dependencies": {
    "firebase": "^12.13.0"
  }
}
